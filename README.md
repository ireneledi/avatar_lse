# avatar_lse
A partir de un audio, se extrae el contenido (speech -> text). El texto se transforma a lenguaje lse (texto -> lse) y la traducción se utiliza para rescatar los gestos correspondientes para el avatar 3D.
